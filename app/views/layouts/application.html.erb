<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "WanderList" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
<div class="top-bar">
  <div class="top-bar-left">
		<h6 class="menu-text"><a href="/">WL</a></h6>
	</div>
  <div class="top-bar-right">
      <% if user_signed_in? %> 
      
        <ul class="menu">
					<li><a href='/trips/'><%= current_user.username %>'s List</a></li>
					<li><%= link_to 'Explore', trips_explore_path %></li>
          <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
        </ul>
   
			<% else %>
		<ul class="menu">
				<li><a data-open="loginmodal">LOGIN</a><%= render "loginmodal" %></li>
				<li><a data-open="signupmodal">SIGNUP</a><%= render "signupmodal" %></li>
			<% end %>
    </ul>
  </div>
</div>
    <%= yield %>
  </body>
</html>
