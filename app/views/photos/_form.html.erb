<%= form_for([@trip, @trip.photos.build]) do |f| %>
  <p>
    <%= f.label :description %>
    <%= f.text_field :description %>
  </p>
  <p>
    <%= f.label :photo_url %>
    <%= f.text_area :photo_url %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>