<p>
 <%= image_tag(photo.photo_url,  :height => 150, :crop => :fill, :class => "thumbnail") %>
</p>
<p>This is in the photo partial</p>
<p>
	<% if current_user.id == @trip.user_id %>
  <%= link_to 'Destroy Photo', [photo.trip, photo],
              method: :delete,
              data: { confirm: 'Are you sure?'} %>
	<% end %>
</p>