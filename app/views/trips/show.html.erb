<h1>Show honeyyy!!!</h1>

<h4>
<%= @trip.city %>, <%= @trip.country %>
</h4>

	<p>
		<div>
		<% if current_user.id == @trip.user_id %>
		<a><%= link_to 'Edit', edit_trip_path %></a>
    <a><%= link_to 'Destroy', trip_path,
              method: :delete,
              data: { confirm: 'Are you sure?' } %></a>
<% end %>
			</div>
	</p>

		
<%= render @trip.photos %>


<!--<= link_to 'Edit', edit_trip_path(@trip) %> |-->

<%= link_to 'Go to your List', trips_path %>
<%= link_to 'Explore more cities', trips_explore_path %>
